<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.devdotdone.ddd.dao.TagDao">
  <!-- 태그 생성 -->
  <insert id="insertTag" useGeneratedKeys="true" keyProperty="tagId" parameterType="Tag">
    <selectKey keyProperty="tagId" resultType="int" order="BEFORE">
      SELECT seq_tag_tagId.NEXTVAL FROM dual
    </selectKey>
    INSERT INTO tag (tag_id, tagName, tagType)
    VALUES (#{tagId}, #{tagName}, #{tagType})
  </insert>

  <!-- 태그 타입별 조회 -->
  <select id="selectTagByType" resultType="Tag">
    SELECT tag_id AS tagId, tagName, tagType
    FROM tag
    WHERE tagType=#{tagType}
    ORDER BY tagName
  </select>

  <!-- 태그 검색 -->
  <select id="searchTags" parameterType="string" resultType="Tag">
    SELECT * 
    FROM tag
    WHERE LOWER(tagName) LIKE CONCAT('%', LOWER(#{keyword}), '%')
    ORDER BY tagName ASC
  </select>
</mapper>