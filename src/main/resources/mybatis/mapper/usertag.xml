<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.devdotdone.ddd.dao.UserTagDao">
  <insert id="insertUserTag">
    INSERT INTO userTag (user_id, tag_id) VALUES (#{userId}, #{tagId})
  </insert>

  <select id="selectTagByUserId" resultType="Tag">
    SELECT t.tag_id, t.tagName, t.tagType
    FROM tag t
    JOIN userTag ut ON t.tag_id = ut.tag_id
    WHERE ut.user_id = #{userId}
  </select>

  <delete id="deleteTagById">
    DELETE FROM userTag
    WHERE user_id=#{userId} AND tag_id=#{tagId}
  </delete>
</mapper>