<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.devdotdone.ddd.dao.ProjectActivityDao">
  <!-- 알림 등록 -->
  <insert id="insertActivity" parameterType="ProjectActivity">
    <selectKey keyProperty="activityId" resultType="int" order="BEFORE"> 
      SELECT seq_projectActivity.nextval from dual 
    </selectKey> 
    INSERT INTO projectActivity (activity_id, paType, sender_id, receiver_id, project_id, paMessage, paStatus, paIsRead, paCreatedAt, paUpdatedAt)
    VALUES (#{activityId}, #{paType}, #{senderId}, #{receiverId}, #{projectId}, #{paMessage}, #{paStatus}, 'N', systimestamp, null)
  </insert>

  <!-- 단건 조회 -->
  <select id="selectById" parameterType="int" resultType="ProjectActivity">
    SELECT activity_id, paType, sender_id, receiver_id, project_id, paMessage, paStatus, paIsRead, paCreatedAt, paUpdatedAt
    FROM projectActivity
    WHERE activity_id = #{activityId}
  </select>

  <!-- 사용자가 받은 알림 조회 -->
  <select id="selectByReceiverId" parameterType="int" resultType="ProjectActivity">
    SELECT pa.*,
      u.userName AS senderName,
      p.projectTitle
    FROM projectActivity pa
    LEFT JOIN users u ON pa.sender_id = u.user_id
    LEFT JOIN project p ON pa.project_id = p.project_id
    WHERE pa.receiver_id = #{receiverId}
    ORDER BY pa.paCreatedAt DESC
  </select>

  <!-- 상태 변경(승인, 거절 등) -->
  <update id="updateActivityStatus" parameterType="ProjectActivity">
    UPDATE projectActivity 
    SET paStatus = #{paStatus}, paIsRead = 'Y', paUpdatedAt = systimestamp
    WHERE activity_id = #{activityId}
  </update>

  <!-- 읽음 상태 업데이트 -->
  <update id="changeToRead" parameterType="int">
    UPDATE projectActivity 
    SET paIsRead = 'Y'
    WHERE activity_id = #{activityId}
  </update>

  <delete id="delete" parameterType="int">
    DELETE FROM projectActivity
    WHERE activity_id = #{value}
  </delete>
</mapper>